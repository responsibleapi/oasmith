env:
  GOCACHE: ${HOME}/.cache/go-build
  GOTMPDIR: ${HOME}/.cache/go-build
taskOptions:
  cache: false
  unixShell: bash
  outputStyle: stream
implicitInputs:
  - /.moon/**/*.yml
  - moon.yml
  - /.github/workflows/ci.yaml
